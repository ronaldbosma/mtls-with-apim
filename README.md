# mtls-with-apim
An azd template using Bicep that demonstrates mutual TLS (mTLS) with Azure API Management. Shows validating client certificates when calling APIM directly and when APIM is behind an application gateway, plus securing APIM-to-backend connections with mTLS.
